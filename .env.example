# Shared auth issuer
AUTH_ISSUER=a2a-local-auth

# Planner-facing client auth
PLANNER_API_AUDIENCE=planner-service

# Executor-facing service auth
EXECUTOR_API_AUDIENCE=code-executor-service
ANALYST_API_AUDIENCE=analyst-agent-service

# Shared Postgres
PLANNER_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/a2a?sslmode=disable
EXECUTOR_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/a2a?sslmode=disable
ANALYST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/a2a?sslmode=disable

# Shared Redis Streams transport
REDIS_URL=redis://127.0.0.1:6379/0
EVENT_STREAM_BLOCK_MS=1000
EVENT_STREAM_MAXLEN=1000

# Planner -> executor discovery
EXECUTOR_AGENT_CARD_URL=http://127.0.0.1:8001/agents/code-executor/.well-known/agent-card.json
ANALYST_AGENT_CARD_URL=http://127.0.0.1:8002/agents/analyst/.well-known/agent-card.json

# Local dev signing key for JWKS + service tokens
AUTH_JWKS_URL=http://127.0.0.1:8000/.well-known/jwks.json
SERVICE_PRIVATE_KEY_PATH=dev_auth/private_key.pem
SERVICE_TOKEN_KID=planner-local-dev-key
SERVICE_TOKEN_SUBJECT=planner-service
SERVICE_TOKEN_TTL_SECONDS=300

# Planner runtime tuning
PLANNER_RECONCILE_INTERVAL_SECONDS=5.0

# Executor queue tuning
EXECUTOR_STEP_DELAY_SECONDS=1.0
EXECUTOR_WORKER_CONCURRENCY=2
EXECUTOR_QUEUE_POLL_SECONDS=0.5
EXECUTOR_QUEUE_LEASE_SECONDS=30
EXECUTOR_QUEUE_RETRY_DELAY_SECONDS=2
EXECUTOR_QUEUE_MAX_ATTEMPTS=3
